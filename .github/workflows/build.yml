name: Get current datetime from default Ubuntu command

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Get current datetime
      run: |
        CURRENTDATETIME=`date +"%A, %b %d, %Y %H:%M:%S"`
        echo Current Date and Time is: ${CURRENTDATETIME}
